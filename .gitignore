node_modules/
.turbo/
dist/
.next/
.workflow-data/
.workflow-vitest/
# Written by Next.js and differs between development and production builds.
apps/*/next-env.d.ts
coverage/
playwright-report/
test-results/

.env
.env.*
!.env.example

# Personal Claude Code overrides. The checked-in settings.json is shared.
.claude/settings.local.json

*.tsbuildinfo
.DS_Store

.yalc/
yalc.lock

# Terraform Cloud holds the state; a local init leaves these behind.
.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
